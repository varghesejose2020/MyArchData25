
name: Update Priests JSON

on:
  schedule:
    - cron: '0 2 * * *'   # once per day
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch data
        run: |
          curl -s -o priests.json https://www.archdiocesechanganacherry.org/index.php/welcome/getpriests

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add priests.json
          git commit -m "Auto update priests data" || echo "No changes"
          git push
